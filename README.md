Structure
```
core/           — sandbox, runner, utils
modules/        — audio, image, video, text
inbox/          — input queue
outbox/         — output queue
intents/        — intent definitions
01osai.py       — main entrypoint
memory_01.py    — simple JSON memory
safety.py       — basic safety filter
shell_01.py     — text command shell
watcher.py      — system observer
```

Run
```
python 01osai.py
```

Commands
```
hi / hello
help
mem
run <python code>
```

Version
v.0001–v.0012 (core prototype)